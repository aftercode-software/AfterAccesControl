import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  placeholderInput: {
    fontFamily: "Poppins_500Medium",
    fontSize: 16,
    color: "#212427", // Cambia esto si necesitas otro color
  },
  // Puedes agregar más estilos globales aquí
});
